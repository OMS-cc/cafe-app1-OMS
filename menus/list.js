(function () { "use strict"; var defaultImages = { "menu-americano": "https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?auto=format&fit=crop&w=700&q=85", "menu-cafe-latte": "https://images.unsplash.com/photo-1509042239860-f550ce710b93?auto=format&fit=crop&w=700&q=85", "menu-matcha-latte": "https://images.unsplash.com/photo-1515823064-d6e0c04616a7?auto=format&fit=crop&w=700&q=85", "menu-earl-grey": "https://images.unsplash.com/photo-1544787219-7f47ccb76574?auto=format&fit=crop&w=700&q=85", "menu-butter-scone": "https://images.unsplash.com/photo-1551024506-0bccd828d307?auto=format&fit=crop&w=700&q=85" }; var labels = { all: "전체", coffee: "커피", "non-coffee": "논커피", tea: "티", dessert: "디저트" }; var activeCategory = "all"; function getMenus() { var stored = CafeUtils.storage.get(CafeData.storageKeys.menus, []); return stored.length ? stored : CafeData.menus.map(function (menu) { return Object.assign({}, menu, { image: menu.image || defaultImages[menu.id] || "" }); }); } function escapeHtml(value) { return String(value || "").replace(/[&<>'"]/g, function (character) { return { "&": "&amp;", "<": "&lt;", ">": "&gt;", "'": "&#39;", '"': "&quot;" }[character]; }); } function renderTabs() { var tabs = [{ id: "all", name: "전체" }].concat(CafeData.categories.map(function (category) { return { id: category.id, name: labels[category.id] || category.name }; })); document.getElementById("category-tabs").innerHTML = tabs.map(function (tab) { return '<button class="category-button" type="button" role="tab" data-category="' + tab.id + '" aria-selected="' + (activeCategory === tab.id) + '">' + tab.name + '</button>'; }).join(""); } function renderMenus() { var term = document.getElementById("search-input").value.trim().toLowerCase(); var menus = getMenus().filter(function (menu) { return (activeCategory === "all" || menu.categoryId === activeCategory) && (!term || menu.name.toLowerCase().includes(term) || String(menu.description || "").toLowerCase().includes(term)); }); var grid = document.getElementById("menu-grid"); grid.innerHTML = menus.map(function (menu) { var image = menu.image || defaultImages[menu.id] || ""; var availability = menu.isAvailable ? "" : '<span class="sold-out">SOLD OUT</span>'; return '<a class="menu-card" href="detail.html?id=' + encodeURIComponent(menu.id) + '"><div class="image-wrap"><img src="' + escapeHtml(image) + '" alt="' + escapeHtml(menu.name) + ' 사진" loading="lazy">' + availability + '</div><div class="menu-information"><span class="menu-category">' + (labels[menu.categoryId] || menu.categoryId).toUpperCase() + '</span><div class="menu-title-row"><h3>' + escapeHtml(menu.name) + '</h3><span>' + CafeUtils.formatPrice(menu.price) + '</span></div><p class="menu-description">' + escapeHtml(menu.description || "정성스럽게 준비한 ORBIT CAFE 메뉴입니다.") + '</p></div></a>'; }).join(""); document.getElementById("empty-state").hidden = menus.length !== 0; if (window.lucide) { lucide.createIcons(); } } document.getElementById("category-tabs").addEventListener("click", function (event) { var button = event.target.closest("button[data-category]"); if (!button) { return; } activeCategory = button.dataset.category; renderTabs(); renderMenus(); }); document.getElementById("search-input").addEventListener("input", renderMenus); renderTabs(); renderMenus(); if (window.lucide) { lucide.createIcons(); } }());
